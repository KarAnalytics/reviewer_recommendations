.env
__pycache__/
*.pyc

# Google credentials -- never push these, regardless of filename.
service_account*.json
*service-account*.json
*.json.key
oauth_client*.json
authorized_user*.json

# Reviewer/submission data (contains reviewer PII and unpublished
# submission content) -- keep it local, never push it to GitHub.
*.xlsx
*.xls
*.log

# Claude session records -- local notes only, not for GitHub.
SESSION_LOG*.md

